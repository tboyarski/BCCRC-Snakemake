#Author:    Tim Boyarski
#Date:	    2017-02-06
#Conda Env: py351wSnake

# Rule specific configurations set in the cluster.json file
# Called:       "snakemake bam/Pfeiffer_realigned_sorted.bam"

rule indexBAM:
    input:
        expand("bam/{samples}_realigned_sorted.bam", samples=config["sample"])
    output:
        expand("bam/{samples}_realigned_sorted.bam.bai", samples=config["sample"])
    shell:
        "~/share/usr/samtools/1.3.1/bin/samtools index {input} {output}"
